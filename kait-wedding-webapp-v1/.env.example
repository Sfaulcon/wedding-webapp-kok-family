# Copy to .env.local (Vite loads .env, .env.local, .env.production)

# Backend API URL — set at BUILD time for production (baked into the bundle).
# Production example: https://api.francoisandkaitlyn.co.za
VITE_API_URL=http://localhost:4000
